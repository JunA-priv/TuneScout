{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Uvicorn (apps.api.main:app)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["apps.api.main:app", "--reload", "--port", "8000"],
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}",
      "jinja": true
    }
  ]
}
